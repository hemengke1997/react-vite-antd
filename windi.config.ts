import { defineConfig } from 'vite-plugin-windicss';

export default defineConfig({
  darkMode: 'class',
  preflight: true,
  theme: {
    extend: {
      colors: {
        // primary:
      },
    },
  },
});
